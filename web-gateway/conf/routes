# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Public urls
GET         /                                        controllers.Application.index()

# API urls
GET         /hello/:id                               controllers.Application.hello(id)
POST        /hello/:id                               controllers.Application.useGreeting(id)
